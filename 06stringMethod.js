let text = "javascript is good";

let CapitalText = text.toUpperCase();
let CapitalFirstLetter = text.charAt(0).toUpperCase();
let SimpleText = text.toLowerCase();

console.log(
  `Capital Text : ${CapitalText} , Simple Text : ${SimpleText} , Capital First Letter : ${CapitalFirstLetter} ${text}`
);
